# bizidealcennetine

# chrome da test için 
flutter run -d chrome --web-browser-flag "--disable-web-security"
# android için 
flutter build apk --release

flutter build appbundle --release --build-name=1.0.30 --build-number=130

# github
git reset --hard HEAD~1
git push origin master --force
